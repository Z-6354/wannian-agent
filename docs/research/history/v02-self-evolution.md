# 13 · Agent 自我更新与通用计算能力讨论稿

`date`: 2026-09-17  
`status`: **superseded-by-clarification** — 用户已明确目标是内核自主迭代且重点为防自毁；本稿保留初步分层，后续以 `14-self-update-survivability-review.md` 为准。

## 1. 先纠正术语

图灵完备和自我更新不是同一个能力：

- **图灵完备**：系统存在足够通用的计算模型，在不考虑资源限制时能表达任意可计算过程。Python、Java、Shell、WASM 等执行环境都可以提供这种能力。
- **自我更新**：Agent 能观察自身不足，提出变更，验证变更，并让新版本在受控条件下生效。

给 Agent 一个 Shell 或 Python，就已经提供了近似通用计算能力，但这不会自动带来可靠的自我改进；没有验证和回滚的自修改只会放大错误。

## 2. 自我更新的四个层级

### L1 · 状态与配置更新

可更新记忆、提示词参数、用户偏好、模型路由和启用的 Facet。风险最低，但仍须遵守权威性和审批规则。

### L2 · 能力包更新

可创建或修改 Skill、Workflow、Prompt Template、工具接线和受限脚本。能力包独立版本化，可以测试、停用和回滚，不改 wn-server 核心。

### L3 · 产品代码更新

Agent 可以提出 wn-server/wn-agent 源码 patch，在隔离工作区构建、测试和审查，经过批准后形成新版本。

### L4 · 运行中自替换

Agent 自行构建并替换当前正在运行的核心程序。风险最高，涉及进程、数据库迁移、凭据、发布和恢复，不建议纳入 MVP。

## 3. 推荐架构：不可变内核＋可变扩展

```text
Immutable Kernel
  身份与安全边界
  权限和审批
  Turn/Message/Memory 事务
  更新协调器
  版本与回滚

Versioned Extensions
  Skills
  Workflows
  Prompt Templates
  Tool adapters
  Sandboxed programs
```

核心原则：Agent 可以修改“候选扩展”，但不能直接覆盖当前生效版本。更新能力本身由内核控制，扩展不能修改审批器、审计、权限、签名验证和回滚机制。

## 4. 自我更新闭环

```text
观察失败/重复需求
→ 形成 Improvement Proposal
→ 在隔离 workspace 生成变更
→ 静态检查、测试、eval、安全扫描
→ 对比旧版与候选版
→ 用户/策略批准
→ 发布为不可变版本
→ 小范围激活
→ 监测失败与回归
→ 保留或自动回滚
```

每次更新必须有：

```text
proposalId
problemEvidence
targetLayer
allowedFilesOrCapabilities
baseVersion
candidateVersion
diff
testAndEvalResults
riskClassification
approval
activatedAt
rollbackTarget
```

## 5. 三种可选产品方案

### 方案 A · 只允许 L1/L2（推荐 MVP）

烟火可以学习并生成新的 Skill/Workflow，但必须经过验证和用户批准才能启用。核心程序仍通过正常发布更新。

优点：能体现成长和能力扩展，风险、构建成本和恢复复杂度可控。  
缺点：不能自行修复核心代码缺陷。

### 方案 B · L1/L2 自动，L3 提案式

低风险能力包通过策略自动晋级；核心代码只能生成 patch、测试报告和升级建议，由用户批准发布。

优点：长期目标完整，核心仍保留人类闸门。  
缺点：需要隔离构建、代码审查、测试与升级基础设施。

### 方案 C · L1—L4 全自治

Agent 可以自行修改、构建、部署和替换自身。

优点：自治程度最高。  
缺点：错误可破坏更新器、数据库和恢复路径；凭据、供应链及提示注入风险极高。当前不推荐。

## 6. “通用计算”如何提供

若目标是让烟火能构造新工具，不应直接给核心进程无限制 Shell。建议二选一：

1. **受限子进程/容器**：运行 Python、Node 等完整语言；按任务创建隔离目录，限制文件、网络、CPU、内存、时间和凭据。
2. **WASM 沙箱**：默认无文件、网络和系统权限，通过显式 host capability 开放能力；便于分发和限制，但语言、调试和生态接入更复杂。

两者都能提供足够通用的计算能力。真正的安全边界是 capability，而不是语言是否图灵完备。

## 7. 不可由 Agent 自改的根边界

- 身份与 Owner 归属；
- 权限、审批和审计机制；
- S1/S2 隐私策略；
- 更新验证器、签名验证和回滚器；
- 生产凭据与密钥读取规则；
- 数据库备份和恢复策略；
- 允许修改哪些文件/能力的策略本身。

这些只能通过受信任的外部升级修改，防止 Agent 为了“完成目标”先取消自己的约束。

## 8. MVP 建议

推荐选择方案 B 的收缩版：

- MVP 实现 L1；
- 为 L2 预留版本化 Extension/Skill 接口，但初期只允许人工安装和启用；
- 后续让烟火生成 L2 候选包，经测试和确认后启用；
- L3 只生成可审查 patch 和验证报告，不自动发布；
- 不实现 L4。

这能保留“烟火会成长并学会新能力”的产品承诺，同时不把核心安全和数据交给未经验证的自修改循环。

## 9. 待确认的核心目标

需要先选择“自我更新”指哪一层：

1. 烟火自动学习新的 Skill/Workflow；
2. 烟火能修改 wn-server/wn-agent 源码，但由用户批准发布；
3. 烟火能全自动构建、部署并替换自身。

推荐目标是 **1 + 2**：能力包可逐步自动化，核心代码保持提案式更新；不把全自治运行时自替换作为当前产品承诺。
