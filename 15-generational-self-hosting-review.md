# 15 · 全内核代际自托管方向讨论稿

`date`: 2026-09-17  
`status`: **brainstorm-draft** — 汇总当前研究进度与建议方向，不表示架构已定案，不授权实施。

## 1. 目标重述

万年不是只允许更新插件、Prompt 或外围工具，而是允许读取、修改和重建包括 Agent Loop、插件加载器、记忆、工具与更新协调器在内的全部 wn-kernel 代码。

核心可靠性目标：候选代可以编译失败、启动失败、行为退化或丧失继续迭代能力，但不能破坏当前稳定代、用户数据和恢复路径。

## 2. 推荐模型

采用“一个身份、多个代际进程”，而不是“一个进程原地手术”：

```text
烟火身份与持久状态（连续）
├─ Generation N：当前稳定内核
├─ Generation N+1：候选内核
└─ Generation N-1：最后可用回退内核
```

N 生成 N+1，N+1 通过后继承同一个烟火身份和状态。它不是另一个人格 Agent，而是烟火的下一代实现。

## 3. A/B 监控器修正

可以设置 guardian A/B，使 A 更新时由 B 存活，B 更新时由 A 存活，但：

- A/B 不能同时更新；
- 活动方只能写非活动 slot；
- 不使用 LLM 自我判断健康，采用确定性探针和 commit token；
- A/B 不共享可被候选原地覆盖的代码目录；
- 需要一个更小的 selector 决定启动 A 还是 B；
- 新 guardian 未 commit 前，selector 默认回到旧 guardian。

这相当于双分区固件升级，而不是两个 Agent 互相聊天检查。

## 4. 最小固定根

所有产品核心均可更新，但无法同时更新“候选”和“唯一能回退候选的代码”。因此保留极小 selector：

```text
读取 slot manifest
启动目标进程
等待健康/commit token
记录启动失败次数
切回 previous slot
```

它不理解对话、记忆或业务，不参与智能决策。它也可 A/B 升级，但最终可靠根落在 OS 服务管理器、容器运行时或 bootloader。这个最小稳定根不是对万年智能的限制，而是让所有智能内核都能安全变化的条件。

## 5. 候选代必须通过的四层验证

1. **构建验证**：可从干净环境重建完整 artifact。
2. **产品不变量**：Turn/SSE/Message、记忆和关系等现有契约不回归。
3. **能力评测**：新需求完成度、历史任务回放、独立未见任务均不下降。
4. **自托管验证**：候选能读取自身、生成一次受控修改、再次构建并启动后代，证明没有失去继续进化能力。

测试与评分器必须来自稳定代或独立证据集；候选不能在同一变更中任意改写自己必须通过的验收标准。

## 6. 与现有资料的关系

- DGM 提供“父代生成子代、archive 保留谱系、benchmark 选择”的自修改核心。
- AHE 提供“所有可编辑组件显式文件化、运行经验分层、修改预测可证伪”的可观测演化。
- DeepSeek Harness 提供“清晰插件契约、不可变 Package、成功后移动 current”的组合能力。
- OpenClaw 与本机 dsh updater 提供“进程外切换、健康驻留、真实回滚和恢复记录”的存活管线。
- HoH 提供“小步迭代、实施测试与独立评价分离、长期版本历史”的持续开发循环。

现阶段没有一个项目将上述机制完整结合；万年的方向是代际自托管，而不是单纯扩大插件系统。

## 7. 下一步需要审核

若接受该方向，下一轮应具体设计：

1. 哪些持久状态属于跨代身份真源；
2. Generation artifact、source、manifest 和谱系如何存放；
3. selector 与 guardian A/B 的最小协议；
4. 影子状态、生产状态和数据库 migration fence；
5. 产品不变量、能力评测与 self-hosting test 的验收集合；
6. 切换、观察和自动回退状态机。

建议先确认：

> 万年采用“一个身份、多个内核代际”的自托管模型。所有 wn-kernel 与 guardian 逻辑都可以按 A/B 代际更新，但永远不原地覆盖当前稳定代；保留一个非智能、极小的 selector 作为跨代启动与回退根。

## 8. 生物式繁衍与教育模型

可以把技术流程表达为“父代生下并教育子代”，而不是父代直接改写自己的活动身体：

```text
受孕 Conception
  父代根据用户需求、运行经验和自身源码定义子代目标

出生 Birth
  在独立源码目录、artifact、进程、端口和影子数据上生成子代

教育 Education
  父代向子代传递身份契约、领域知识、工具使用、历史故障和工作方法

实习 Apprenticeship
  子代处理历史任务回放、模拟请求和影子流量，结果不直接影响用户

毕业 Graduation
  独立考官执行产品不变量、未知任务、故障注入、重启恢复和自托管测试

继承 Succession
  selector 将新请求切给子代；父代保持只读待命，不立即销毁

观察 Probation
  子代在真实运行窗口内持续接受健康和语义检查

成年 Commit
  子代成为新的稳定代，可以繁衍下一代

退役 Retirement
  父代退出服务，但 artifact、谱系和必要恢复能力继续保留
```

### 8.1 父代应该传递什么

- CompanionIdentity、Owner 关系和不可变量；
- 当前稳定代码和架构说明；
- 经过筛选的 Memory、运行日志和失败案例；
- 用户提出的新能力需求及验收场景；
- 父代已知的自身缺陷和未解决问题；
- 如何继续生成、教育和验证下一代的自托管能力。

这些内容通过版本化的 `InheritanceBundle` 传递，不依赖父代在 Prompt 中临时口述。

### 8.2 子代不能简单复制父代

如果父代同时定义答案、教育过程和毕业标准，父代的盲点会原样遗传。因此：

- 父代可以生成课程和候选实现，但不能独占最终评分；
- 固定产品不变量由版本化测试和 selector/guardian 检查；
- 使用父代未见过或无法修改的 sealed evaluation 样本；
- 让子代解释修改预测，再用实际结果核对，而不是接受自评；
- 保留祖先版本和多条候选分支，避免只沿一个错误谱系继续进化。

独立考官不必是另一个人格 Agent，可以是确定性测试、协议探针、历史回放、故障注入器和冻结评测集。

### 8.3 继承的是身份，不是进程

用户仍然面对同一个烟火。子代接管的是同一个 `CompanionIdentity` 和经过兼容迁移的持久状态；代际编号属于实现元数据，不创建第二个角色或第二套关系。

建议区分：

```text
companionIdentityId = 永久不变的“烟火”
kernelGenerationId  = 当前实现代际
lineageParentId     = 该内核由哪一代产生
```

### 8.4 父代何时可以退出

子代必须同时满足：

1. 可以独立启动、停止和重启恢复；
2. 可以处理真实 Turn 与流式输出；
3. 能读取并正确解释继承状态；
4. 完成新需求验收和既有回归；
5. 通过故障注入；
6. 能生成一个最小孙代并完成构建，证明繁衍能力仍在；
7. 经过真实观察窗口且错误率未越界。

切换后父代仍保留一段回退窗口。只有子代完成成年 commit，父代才从活动待命转为归档；不能在刚切换时立即销毁父代。

### 8.5 推荐产品术语与工程术语

用户叙事可以使用“成长、传承、新生代”；工程文档仍使用：

```text
stable generation
candidate generation
inheritance bundle
graduation suite
probation window
succession commit
ancestor rollback
```

这样既保留生物式直觉，也避免实现时把拟人化描述误当作事务和状态定义。
