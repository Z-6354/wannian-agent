# wn-server-kernel

Domain kernel: turns, agent loop, memory/relationship policy, tool policy.

**Depends on:** `wn-server-api` only.  
**Forbidden:** Spring MVC / Boot, Controllers, SQLite drivers, vendor model clients.
