# wn-server-app

Spring Boot host: HTTP/SSE, SQLite adapters, model adapters, process lifecycle.

**Depends on:** `wn-server-api` + `wn-server-kernel`.  
Adapters convert technology concerns; they must not re-implement domain policy.
