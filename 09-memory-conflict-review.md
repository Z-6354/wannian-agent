# 09 · 记忆冲突与时间语义审核草案

`date`: 2026-09-17  
`status`: **confirmed** — 四种冲突结果及基本召回语义已于 2026-09-17 确认，不授权实施。

## 1. 问题

“新记忆与旧记忆不一致”不一定是真冲突，至少可能是：

1. 用户纠正了旧事实；
2. 事实随时间发生变化；
3. 不同情境下偏好不同；
4. 来源表达含糊，无法判断；
5. 两条记录确实互相排斥。

如果一律追加，会让召回结果互相矛盾；如果一律覆盖，又会丢失历史、上下文和用户纠正过程。

## 2. 推荐的四种处理结果

```text
SUPERSEDE   新内容替代旧内容，保留替代链
COEXIST     两条内容在不同情境下同时成立
TEMPORAL    同一事实在不同时间段分别成立
HOLD        无法安全判断，保留候选等待确认
```

物理删除不属于普通冲突处理。只有用户明确遗忘、数据保留政策或安全要求才删除正文；通常用生命周期与关系链保留可解释历史。

## 3. 决策规则

### 3.1 用户明确纠正：SUPERSEDE

示例：

```text
旧：用户希望被称为“小韩”
新：以后不要叫我小韩，叫我阿万
```

- 新记录进入 `ACTIVE`；旧记录进入 `SUPERSEDED`。
- 保存 `supersedesMemoryId` 和纠正来源 Message。
- Prompt 只召回新记录；历史页可以显示替代链。
- 如果用户纠正的是烟火的错误推断，新内容权威性高于旧推断。

### 3.2 事实随时间变化：TEMPORAL

示例：

```text
2026-09：用户在上海工作
2027-03：用户已搬到杭州工作
```

- 旧记录不是“错误”，而是有效期结束。
- 建议记录 `validFrom`、`validTo`；当前召回默认取有效区间覆盖现在的记录。
- 没有明确时间时，以来源 Turn 时间作为最低限度的观测时间，不能假装知道精确生效时间。
- 历史问题可以检索旧记录，当前偏好注入只使用当前有效记录。

### 3.3 情境不同：COEXIST

示例：

```text
工作回复偏好简洁
情感陪伴时偏好更温柔、展开一些
```

- 两条都保持 `ACTIVE`，但必须有不同 context/facet 条件。
- 没有可表达的情境条件时，不允许用 COEXIST 掩盖真正矛盾。
- 召回时先匹配 Facet、任务和 Conversation 场景，再决定注入哪条。

### 3.4 无法判断：HOLD

示例：用户一次说“我不喝咖啡”，后来在含糊语境中说“咖啡还行”。

- 新记录保持 `CANDIDATE`，不改变旧 `ACTIVE`。
- 可以等待更多证据；若该事实会明显影响体验，可在自然语境中向用户确认。
- 不允许让 LLM 用 confidence 高低静默裁决关系、身份或敏感事实冲突。

## 4. 权威优先级

冲突裁决的默认权威顺序：

```text
用户当前明确纠正
> 用户明确要求记住的陈述
> 用户直接明确陈述
> 多 Turn 一致观察
> INFERRED
> REFLECTION
> Assistant 自己的历史表述
```

权威顺序用于选择处理路径，不允许跨过时间和情境判断。例如新陈述与旧陈述不同，应先判断是变化还是纠错，而不是仅因“新”就覆盖。

## 5. 数据关系建议

记忆本体与冲突关系分开：

```text
Memory
  id
  content
  lifecycle
  validFrom?
  validTo?
  contextQualifier?

MemoryRelation
  fromMemoryId
  relationType   SUPERSEDES | REFINES | CONTRADICTS | CONTEXTUALIZES
  toMemoryId
  sourceTurnId
  createdAt
```

MVP 可以先只实现 `SUPERSEDES` 和 context/facet 条件，但 API/表意上不要把“替代”和“删除”混为一谈。

## 6. 召回不变量

- 默认 Prompt 不召回 `SUPERSEDED / REJECTED / FORGOTTEN` 正文。
- 当前事实召回优先匹配有效时间、Facet 和情境。
- 同一事实槽出现多个互斥 `ACTIVE` 且无法按情境消解时，视为数据异常，不得随机挑选。
- 冲突处理必须幂等；同一来源任务重试不能反复生成替代链。
- 遗忘墓碑优先于旧摘要和历史 Conversation，禁止旧信息被重新摄取。

## 7. 已确认决策

> MVP 采用 SUPERSEDE、COEXIST、TEMPORAL、HOLD 四种冲突结果。明确纠正走替代链；随时间变化保留有效期；情境差异按 Facet/条件并存；无法判断留在候选。普通冲突不物理删除历史。

如果希望压缩 MVP，时间字段可以先允许为空，但不能用简单覆盖代替上述语义。
