# 08 · 敏感记忆分类与保存边界审核草案

`date`: 2026-09-17  
`status`: **confirmed** — S0/S1/S2 产品语义已于 2026-09-17 确认；加密与密钥实现仍待后续设计，不授权实施。

## 1. 核心问题

“敏感内容必须确认”仍然过于宽泛。必须区分：允许自动保存、需要用户明确要求才保存、以及即使用户要求也不应作为普通长期记忆保存的内容。

## 2. 推荐三级模型

### S0 · 普通内容

可以在其他自动提升门槛通过后进入 `ACTIVE`：

- 称呼、语言、展示格式；
- 普通兴趣和日常偏好；
- 工作流、常用工具、创作习惯；
- 用户主动说明的非敏感日程习惯；
- 对产品行为的稳定偏好。

### S1 · 私密但可保存

不得由观察或推断自动提升。只有用户明确要求“记住”并看到保存提示后，才能进入加密存储：

- 健康状况、用药和心理状态；
- 财务状况、收入、债务和消费约束；
- 精确住址、经常活动地点和可定位行程；
- 家庭、亲密关系、性相关信息；
- 宗教、政治立场、民族等敏感身份；
- 身份证件的非凭据性描述。

保存时应记录明确同意的来源 Message，并提供单条删除和整类清除入口。S1 默认不得进入日志、分析事件或普通导出。

### S2 · 禁止作为长期记忆保存

即使用户说“记住”，MVP 也不应写入长期记忆：

- 密码、验证码、恢复码、API key、私钥；
- 完整银行卡号、安全码、支付口令；
- 身份证、护照等完整证件号码及证件影像；
- 可直接用于登录、授权、冒充或资金操作的信息；
- 用户明确要求仅本次使用或阅后即焚的内容。

这些内容可以在完成当前任务所需的最短生命周期内存在于受控 Turn 上下文，但不得进入候选、向量索引、摘要、Reflection 或调试日志。

## 3. 与来源类型的组合规则

| 来源 | S0 | S1 | S2 |
|---|---|---|---|
| EXPLICIT，明确要求记住 | 可 ACTIVE | 明示提示后加密 ACTIVE | 拒绝长期保存 |
| EXPLICIT，仅陈述 | 可按门槛提升 | CANDIDATE 或不抽取 | 不抽取 |
| OBSERVED | 可按门槛提升 | 不自动提升 | 不抽取 |
| INFERRED | 跨 Turn 证据后才可能提升 | 禁止提升 | 禁止抽取 |
| REFLECTION | 不作为事实覆盖 | 禁止形成敏感事实 | 禁止抽取 |

## 4. 审核意见

推荐 MVP 采用以下不变量：

1. 敏感度先于 confidence；高置信不能降低敏感等级。
2. 抽取器在生成候选之前先做 S2 过滤，避免秘密先落库再删除。
3. S1 与 S0 使用不同加密和召回策略；S1 不参与宽泛语义检索，只允许受控精确召回。
4. Prompt 注入 S1 时，应按当前意图进行最小披露，不能因为“与用户有关”就全部载入。
5. 用户遗忘 S1 时，删除正文、向量和派生摘要，只保留不含原文的排除指纹/墓碑。
6. 不从 Assistant 猜测、关系 Reflection 或外部网页内容创建用户敏感事实。

## 5. 已确认决策

> MVP 使用 S0/S1/S2 三级敏感度。S1 只有用户明确要求记住并收到可见提示后才加密保存；S2 永不进入长期记忆，即使用户明确要求也拒绝保存。

后续还需单独设计 S1 的加密密钥、备份、导出和删除实现；本草案只确定产品语义，不提前决定技术实现。
